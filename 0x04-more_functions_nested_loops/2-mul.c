#include<unistd.h>
#include "main.h"
/**
 *mul - multiplies 2 integers
 *
 *@a:The first digit
 *@b:The second digit
 *
 * Return: 1 for digit. 0 for the rest.
 */
int mul(int a, int b)
{
	return (a * b);
}
