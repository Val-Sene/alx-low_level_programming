#include <stdio.h>
#include <stdlib.h>

/**
 * main - print the name of the file used in the compiling process
 *
 * Return: Always success
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
